<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ data.siteTitle + " - " + title }}</title>
    <link rel="stylesheet" href="/css/mvp.css">
  </head>
  <body class="lightMode">
    <main>
      {{ content | safe }}
          </main>
  </body>
</html>
